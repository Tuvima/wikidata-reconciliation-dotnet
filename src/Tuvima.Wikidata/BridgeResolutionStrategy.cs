namespace Tuvima.Wikidata;

/// <summary>
/// Strategy that produced a bridge resolution result.
/// </summary>
public enum BridgeResolutionStrategy
{
    NotResolved = 0,
    BridgeId,
    TextSearch
}
